# Third-Party Notices

This project (Local AI Deploy) is released under the MIT License — see
[LICENSE](LICENSE). That license covers **this project's own code only**.

Third-party components are **not** relicensed by this project:

- **llama.cpp runtime binaries** are downloaded at install time from the
  official [ggml-org/llama.cpp](https://github.com/ggml-org/llama.cpp)
  releases and remain under their upstream MIT license. This project does not
  modify, redistribute, or relicense them.
- **GGUF model files** are downloaded from their respective Hugging Face
  repositories and remain under the licenses recorded in `models/*.json`.
  This project makes no ownership claim over model weights or over
  model-generated output.

The MIT license of this repository does **not** cover model weights, runtime
binaries, or any output generated by a deployed model.

Each curated model manifest in `models/*.json` records the upstream source
repository, pinned revision, license and verification metadata, so attribution
stays attached to the artifact it belongs to.
